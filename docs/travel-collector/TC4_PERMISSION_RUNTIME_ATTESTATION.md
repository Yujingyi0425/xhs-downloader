# TC4 Permission Runtime Attestation

```text
PHASE=TC4-REAL-VIDEO-NAVIGATION-GRACE-PREDICATE-PERMISSION-REPAIR
MODE=NON-CANARY-RUNTIME-PERMISSION-ATTESTATION
STATUS=HOLD_CANONICAL_XHS_TAB_NOT_VISIBLE
CURRENT_HEAD=1c891e7a8c323aedcca8e9b38b611d21ab8c20ba
LOCAL_REMOTE_MATCH=YES
WORKTREE_CLEAN=YES
```

## Runtime identity

本次只使用现有 canonical 外部浏览器控制面和本机 API 状态；没有使用普通 Node context
中的 `globalThis.chrome` 作为 gate，也没有创建或执行任何任务。

```text
CANONICAL_EXTENSION_RUNTIME_ACTIVE=PASS
EXTENSION_WORKER_REGISTERED=PASS
EXTENSION_WORKER_AVAILABLE=PASS
EXTENSION_TO_LOCAL_API_CHANNEL=PASS
LOCAL_XHS_API_READY=PASS
CURRENT_XHS_TAB_VISIBLE=NO
CURRENT_XHS_TAB_SELECTABLE=NO
LOGIN_STATE_VISIBLE=NO
```

本机 API 健康检查为 `ok`，canonical extension registration 存在且在线；浏览器控制
面 `tabs.list` 和 `user.openTabs` 均返回 0 个标签页。因此当前 XHS 页面和登录状态不能
被本次控制面独立观察。

## Permission effect

```text
SOURCE_MANIFEST_XHS_HOST_PERMISSION=PASS
DIST_MANIFEST_XHS_HOST_PERMISSION=PASS
TAB_URL_PROPERTY_ACCESSIBLE=NOT_PROVEN
TAB_URL_HOST_IS_XHS=NOT_PROVEN
PENDING_URL_RUNTIME_OBSERVED=NO_NATURAL_IN_FLIGHT_NAVIGATION
RUNTIME_PERMISSION_ATTESTATION=HOLD_CURRENT_XHS_TAB_UNAVAILABLE
XHS_HOST_PERMISSION_RUNTIME_EFFECTIVE=NOT_PROVEN
PERMISSION_DEFECT_REPAIR_PROVEN=NOT_PROVEN
R4E_GRACE_PREDICATE_END_TO_END_EFFECTIVENESS=NOT_YET_PROVEN
R4E_READINESS_SEMANTICS_UNCHANGED=PASS
R4E_TIMEOUT_SEMANTICS_UNCHANGED=PASS
```

没有读取或记录 raw URL query、xsec_token、signed URL、Cookie、Authorization 或其他
secret。没有为了获得 pendingUrl 创建新页面或任务。

## Security recheck

```text
TC4_SECRET_PERSISTENCE=PASS
TC4_TOKEN_PERSISTED=NO
RAW_PENDING_URL_PERSISTED=NO
SIGNED_MEDIA_URL_PERSISTED=NO
RAW_URL_QUERY_PERSISTED=NO
COOKIE_PERSISTED=NO
AUTHORIZATION_PERSISTED=NO
REAL_MEDIA_COMMITTED=NO
REAL_USER_DATA_COMMITTED=NO
SECRET_COMMITTED=NO
```

安全复核基于现有本地 synthetic/runtime-safe evidence；未使用新的 real task。

## Final checkpoint

```text
TOTAL_PHYSICAL_CANARY_COUNT=5
NEW_PHYSICAL_CANARY_EXECUTED=NO
TC4_OVERALL_PASS=NO
TC5_AUTHORIZED=NO
TC6_AUTHORIZED=NO
```

当前证据不足以把 permission runtime attestation 标记为 PASS。到此停止；后续若继续，
只需让现有 canonical XHS 标签页出现在浏览器控制面后重新核验，不得执行新的 physical
canary、real-video retry、GET_FEED_MEDIA 或 media acquisition。
